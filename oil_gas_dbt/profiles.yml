oil_gas_dbt:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'oilgas') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'oilgas') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'oilgas') }}"
      schema: public
      threads: 4